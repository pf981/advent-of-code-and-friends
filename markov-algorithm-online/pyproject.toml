[project]
name = "markov-algorithm-online"
version = "1.0.0"
description = "Solutions to https://mao.snuke.org/"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "pytest>=8.4.1",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
    "ruff>=0.12.3",
    "ty>=0.0.5",
]

[tool.pytest.ini_options]
pythonpath = "."
