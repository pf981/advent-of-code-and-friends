package solutions

import "../utils"

y2025_d01 := utils.Solution(string, string) {
	year = 2025,
	day = 1,
	parse = proc(input: string) -> string {
		return ""
	},
	part1 = proc(input: string) -> string {
		return ""
	},
	part2 = proc(input: string) -> string {
		return ""
	},
}
