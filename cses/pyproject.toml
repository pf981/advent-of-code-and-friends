[project]
name = "cses"
version = "1.0.0"
description = "Solutions to https://cses.fi/"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ipykernel>=6.31.0",
    "pytest>=8.4.2",
    "requests>=2.32.5",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
